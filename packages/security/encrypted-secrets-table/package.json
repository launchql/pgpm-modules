{
  "name": "encrypted-secrets-table",
  "version": "0.1.0",
  "description": "Encrypted secrets backing table helpers",
  "main": "./index.js",
  "types": "./index.d.ts",
  "module": "./esm/index.js",
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "scripts": {
    "copy": "cpy ../../../LICENSE README.md package.json dist --flat && cpy \"schemas/**\" \"sql/**\" \"*.control\" \"launchql.plan\" \"sqitch.conf\" dist --parents --cwd . && sh -c 'for d in deploy verify revert; do if [ -d \"$d\" ]; then cp -a \"$d\" dist/; fi; done'",
    "clean": "rimraf dist",
    "prepare": "npm run build",
    "build": "npm run clean; tsc; tsc -p tsconfig.esm.json; npm run copy",
    "test": "jest --config ./jest.config.js",
    "test:watch": "jest --config ./jest.config.js --watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/launchql/extensions"
  },
  "homepage": "https://github.com/launchql/extensions",
  "bugs": {
    "url": "https://github.com/launchql/extensions/issues"
  }
}
