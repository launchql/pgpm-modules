{
  "name": "@pagebond/my-third",
  "version": "0.2.0",
  "description": "Core functionality",
  "main": "./index.js",
  "types": "./index.d.ts",
  "module": "./esm/index.js",
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "scripts": {
    "copy": "cpy ../../LICENSE README.md package.json dist --flat",
    "clean": "rimraf dist",
    "prepare": "npm run build",
    "build": "npm run clean; tsc; tsc -p tsconfig.esm.json; npm run copy",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@pagebond/my-first": "workspace:^",
    "@pagebond/my-second": "workspace:^"
  }
}
