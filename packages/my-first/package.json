{
  "name": "@pagebond/my-first",
  "version": "0.2.0",
  "description": "Core functionality",
  "main": "./index.js",
  "types": "./index.d.ts",
  "module": "./esm/index.js",
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "scripts": {
    "copy": "cpy ../../LICENSE README.md package.json dist --flat",
    "clean": "rimraf dist",
    "prepare": "npm run build",
    "build": "npm run clean; tsc; tsc -p tsconfig.esm.json; npm run copy",
    "test": "jest --config ./jest.config.js --passWithNoTests --testPathIgnorePatterns=/dist/",
    "test:watch": "jest --config ./jest.config.js --watch --testPathIgnorePatterns=/dist/"
  }
}
